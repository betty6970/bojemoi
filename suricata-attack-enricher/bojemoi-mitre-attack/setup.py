from setuptools import setup, find_packages

setup(
    name="bojemoi-mitre-attack",
    version="1.0.0",
    description="MITRE ATT&CK mapping library for Bojemoi Lab",
    packages=find_packages(),
    python_requires=">=3.11",
    install_requires=[],
)
